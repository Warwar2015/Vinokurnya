<UserControl x:Class="VinokurnyaWpf.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:VinokurnyaWpf.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.DataContext>
        <vm:StatisticsViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <ScrollViewer>
            <StackPanel>
                <!-- Overall Stats -->
                <Border BorderBrush="{DynamicResource PrimaryBrush}" 
                        BorderThickness="2" 
                        CornerRadius="10" 
                        Padding="20"
                        Background="{DynamicResource PrimaryBrush}"
                        Opacity="0.1"
                        Margin="0,0,20,0">
                    <StackPanel>
                        <TextBlock Text="Общая статистика" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                        <Grid Margin="0,15,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Всего рецептов:" FontWeight="Bold" Margin="0,0,15,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalRecipesCount}" FontSize="20" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Рецептов в избранном:" FontWeight="Bold" Margin="0,0,15,5"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding FavoriteRecipesCount}" FontSize="20" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Всего заметок:" FontWeight="Bold" Margin="0,0,15,5"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TotalNotesCount}" FontSize="20" VerticalAlignment="Center"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Средний рейтинг:" FontWeight="Bold" Margin="0,0,15,5"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding AverageRating}" FontSize="20" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Favorite Recipes -->
                <TextBlock Text="🔥 Рецепты в избранном" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>
                <Border BorderBrush="{DynamicResource PrimaryBrush}" 
                        BorderThickness="2" 
                        CornerRadius="10" 
                        Padding="20"
                        Background="{DynamicResource PrimaryBrush}"
                        Opacity="0.1">
                    <ScrollViewer MaxHeight="300">
                        <ItemsControl ItemsSource="{Binding FavoriteRecipes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="★" FontWeight="Bold" FontSize="18" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="10,0,5,0"/>
                                        <TextBlock Text="(" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Rating, StringFormat='F1'}" Foreground="Gray"/>
                                        <TextBlock Text=")" Foreground="Gray"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- Most Used Category -->
                <Border BorderBrush="{DynamicResource PrimaryBrush}" 
                        BorderThickness="2" 
                        CornerRadius="10" 
                        Padding="20"
                        Background="{DynamicResource PrimaryBrush}"
                        Opacity="0.1"
                        Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="📊 Самая популярная категория" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                        <TextBlock Text="{Binding MostUsedCategory}" FontSize="18" Margin="0,15,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Instructions -->
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Text="💡 Информация:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="• Рецепты в избранном помечены значком ★" FontSize="14" Margin="0,5,0,0"/>
                    <TextBlock Text="• Рейтинг рассчитывается на основе оценок пользователей" FontSize="14" Margin="0,5,0,0"/>
                    <TextBlock Text="• Используйте вкладки для навигации по функциям приложения" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>